# This file marks this directory as a Python package.
# It enables the relative imports (e.g. "from .constants import ...") used by the
# modules in this folder. Without it, Python would not treat this directory as a
# package and those imports would fail.
#
# You do not need to add any code here — its presence is sufficient.
